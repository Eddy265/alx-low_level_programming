#include <stdio.h>
/**
 * main - This is my main function
 *
 * Return: return 0 if successful
 */

int main(void)
{
	char i;
	/* my main function goes here*/

	for (i = 'a'; i <= 'z'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
